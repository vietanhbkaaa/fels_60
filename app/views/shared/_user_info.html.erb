<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span>
  Learned
  <%= pluralize(current_user.lessons.map{ |lesson| lesson.answers.count }.sum, "word") %>
</span>
